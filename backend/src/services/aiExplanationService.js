// ─── AI Explanation Service ───────────────────────────────
// Integrates OpenAI/Gemini to explain code files and modules
// Full implementation → Day 68-74 (AI Integration)

// Placeholder — AI integration coming Week 10
export const generateFileExplanation = async (fileContent) => {
  console.log(`🤖 AI explanation service initialized`);
  return {
    summary: "AI explanation will be generated here.",
    explanation: null,
  };
};

export const generateArchitectureExplanation = async (graphData) => {
  console.log(`🤖 Architecture explanation service initialized`);
  return {
    summary: "Architecture explanation will be generated here.",
    patterns: [],
  };
};